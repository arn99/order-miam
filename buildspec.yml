version: 0.2

phases:
  pre_build:
    commands:
      - npm install newman --global
  build:
    commands:
      - newman run -r html,cli ./newman/order.postman_collection.json -e ./newman/order.postman_environment.json --reporter-html-export report.html

artifacts:
  files:
    - report.html